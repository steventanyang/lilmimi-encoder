[project]
name = "lilmimi-encoder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.3",
    "torch>=2.14.0",
]

[dependency-groups]
dev = [
    "einops>=0.8.2",
    "huggingface-hub>=2.0.0",
    "safetensors>=0.8.0",
]
